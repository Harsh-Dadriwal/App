{
  "name": "mahalaxmi-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "packages/core"
  ],
  "scripts": {
    "build:api": "npm run build --workspace=apps/api",
    "start:api": "npm run start --workspace=apps/api"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
